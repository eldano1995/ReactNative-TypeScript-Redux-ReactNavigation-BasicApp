// const rootReducer: any = { text: "Hello World" }
const rootReducer = (state: any = {}, action: any) => {
    switch (action.type) {
        default:{
            return state
        }
    }
}

export default rootReducer;